# Contributing

This repository is in architecture/starter stage.

Before accepting external contributions:

1. select a project license;
2. add a code of conduct if desired;
3. define supported deployment versions;
4. require tests for provisioning logic;
5. never commit API keys, mailbox passwords, DKIM private keys, backup keys, or customer data.

Security-sensitive changes should receive additional review.
